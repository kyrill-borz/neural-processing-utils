# Utils folder
These are the functions used by the neural signal processing softwares.
Import these by running:

```python
git clone https://github.com/kyrill-borz/neural-processing-utils.git utils
```
That will clone all the functions into a folder titled utils
